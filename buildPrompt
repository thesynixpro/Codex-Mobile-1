debug-apk-build-GITHUB_RUN_NUMBER-GITHUB_RUN_ATTEMPT

Target commit:

GITHUB_SHA

Release notes that clearly state:

* This is an automatically generated debug APK.

* The APK was built by GitHub Actions.

* The GitHub Actions run number.

* The GitHub Actions run attempt.

* The source commit SHA.

* The APK is intended for testing.

* The APK is not a Play Store production release.

* The APK is signed only with a temporary debug keystore.

Attach only the prepared .apk file as the manually uploaded release asset.

Do not manually attach:

* A ZIP file

* A source-code archive

* A tar.gz file

* The debug keystore

* Credentials

* Configuration files

* Gradle files

* Build caches

* Logs

* Secrets

* Any APK other than the verified app debug APK

GitHub may automatically display its standard Source code (zip) and Source code (tar.gz) links for the release. Do not attempt to create, upload, replace, remove, or modify those platform-generated links.

Mark the newly created release explicitly as the latest release using:

--latest

Do not create the release as:

* A draft

* A prerelease

* An immutable release requiring later manual publication

The APK must be visible directly under the release Assets section after successful publishing.

Release failure protection

The GitHub Actions artifact must be uploaded before release publishing begins.

Configure the release publishing step so that:

* A release error does not remove or hide the successfully uploaded Actions artifact.

* A release error does not cause the APK build result to be lost.

* A release error is shown clearly as a warning.

* The workflow summary clearly reports that the APK build and artifact upload succeeded even when release publishing fails.

* The workflow does not falsely report that release publishing succeeded.

* The release step uses continue-on-error: true.

* No unrelated fallback release action is attempted.

* No duplicate release is created as a fallback.

Assign an id to the release step so its outcome can be checked later.

Verify the release result

After the release attempt, add a step named:

Report APK download result

This step must always run after the release attempt.

If release publishing succeeded:

* Confirm the release using gh release view.

* Print the release tag.

* Print the release title.

* Print the direct GitHub Release page URL.

* Print the exact APK asset filename.

* Add the release page URL to GITHUB_STEP_SUMMARY.

* Add a message explaining that the APK can be downloaded from the release Assets section.

* State that the normal Actions artifact is also available as a backup.

If release publishing failed:

* Print a clear warning.

* State that the APK build succeeded.

* State that the Actions artifact named app-debug-apk remains available.

* Add the same warning and backup instructions to GITHUB_STEP_SUMMARY.

* Do not falsely print a release URL.

* Do not fail the completed APK build solely because release publishing failed.

Authenticate any gh command in this reporting step using:

GH_TOKEN: ${{ github.token }}

Security restrictions

Do not create fake:

* google-services.json files

* Firebase files

* API keys

* Credentials

* Personal access tokens

* Release-signing files

* Production keystores

* Missing secrets

* Placeholder secrets

Do not expose:

* GITHUB_TOKEN

* GH_TOKEN

* Environment credentials

* Repository secrets

* Keystore contents

Do not echo authentication tokens into workflow logs.

Do not persist credentials beyond the temporary GitHub Actions runner.

Do not modify repository settings.

Do not enable GitHub Pages.

Do not create a website.

Do not create a second workflow.

Do not create a release manually while preparing the repository.

The release must be created only when the generated workflow runs after I manually commit and push the workflow file.

Final validation before finishing

Before finishing, confirm all of the following:

* Only .github/workflows/build-apk.yml was created.

* No existing file was modified.

* AGP 9.1.1 was confirmed from gradle/libs.versions.toml.

* Java 17 is configured before Gradle.

* The active Java version is verified.

* Gradle 9.3.1 is used exactly.

* The repository Gradle wrapper is not used.

* The temporary debug keystore is created before the build.

* The temporary debug keystore is never uploaded.

* The build command is exactly gradle :app:assembleDebug --stacktrace --no-daemon.

* The APK path is exactly app/build/outputs/apk/debug/app-debug.apk.

* The Actions artifact name is exactly app-debug-apk.

* actions/upload-artifact is configured to fail when the APK is missing.

* The Actions artifact is uploaded before release publishing.

* The release asset is an actual .apk file and not a ZIP.

* Every workflow execution receives a unique release tag.

* GITHUB_RUN_NUMBER and GITHUB_RUN_ATTEMPT are both used in the tag.

* The release targets GITHUB_SHA.

* The release is explicitly marked as latest.

* The built-in GitHub token is used.

* The workflow has contents: write permission.

* No personal access token or repository secret is required.

* Release failure does not remove the Actions artifact.

* The workflow summary reports whether release publishing succeeded or failed.

* No Android application or project file was modified.

* No file other than .github/workflows/build-apk.yml was created.

Give me a short workflow summary that includes:

* The confirmed AGP version.

* The Java version requirement.

* The exact Gradle version.

* The exact build command.

* The APK path.

* The Actions artifact name.

* The release tag format.

* The release asset filename format.

* Confirmation that the latest successful release will contain a directly downloadable APK.

* Confirmation that release failure will leave the Actions artifact available.

Do not publish, commit, sync, push, tag, or create a GitHub Release now.

Wait for me to manually commit and push the workflow file to GitHub
